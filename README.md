# 🤖 Multi-Agent System — AI-Powered Orchestration & Collaboration Platform

> **Multi-Agent System** — это передовая веб-платформа для проектирования, настройки и выполнения сложных сценариев взаимодействия интеллектуальных ИИ-агентов с ролевыми моделями. Проект представляет собой полноценное Full-Stack приложение, спроектированное по архитектуре **Monorepo** (монорепозиторий). Интерфейс включает интерактивный визуальный граф связей (XYFlow/React Flow), изолированную песочницу для выполнения кода и продвинутую RAG-систему работы с документами.

---

## 🚀 Стек технологий / Tech Stack

*   **Backend Core:** Python 3.11+, FastAPI (асинхронная разработка, высокая производительность), Uvicorn.
*   **Database & RAG:** SQLite + SQLAlchemy 2.0 (асинхронный драйвер `aiosqlite`), ChromaDB (векторное хранилище) с кастомным TF-IDF / Cosine Similarity фолбеком для отказоустойчивости.
*   **Frontend Core:** Next.js 15+ (App Router), React 19, TypeScript (строгая типизация), Zustand (минималистичное хранилище состояния).
*   **Styling & UI:** Tailwind CSS + Vanilla CSS (для кастомных визуальных решений), XYFlow/React (построение интерактивных визуальных связей между агентами), Framer Motion (плавные анимации и переходы).
*   **AI Integrations:** OpenRouter API (асинхронная отправка запросов и стриминг ответов LLM).
*   **Auth & Security:** JWT (JSON Web Tokens) с использованием библиотеки `PyJWT` для безопасной аутентификации.

---

## ✨ Ключевые возможности / Core Features

### 1. 📊 Визуальный граф воркфлоу (Visual Agent Graph)
*   Проектирование связей и сценариев взаимодействия между агентами с использованием интерактивного холста на базе XYFlow.
*   Стилизованные анимированные ребра связей (`AnimatedEdge.tsx`) для наглядного отображения активных потоков обмена сообщениями и зависимостей в реальном времени.

### 2. 🧠 Мультиагентный оркестратор (Multi-Agent Orchestrator)
*   Ядро оркестрации (`orchestrator.py`), управляющее памятью сессии, системными промптами агентов, ролевыми моделями и распределением задач.
*   Асинхронный стриминг ответов LLM через OpenRouter API для бесшовного вывода текста в реальном времени.

### 3. 🛡️ Защищенная песочница кода (Secure Python Sandbox)
*   Выделенный модуль `sandbox.py` для безопасного выполнения сгенерированного агентами Python-кода непосредственно в серверном окружении.
*   Динамический захват стандартного вывода (stdout/stderr) с последующей трансляцией результатов и ошибок во встроенную панель терминала.

### 4. 📂 Интеллектуальный RAG-модуль (Document Retrieval System)
*   Поддержка чтения и парсинга документов форматов PDF (через `PyPDF`), Excel (`pandas`), CSV и обычных текстовых файлов.
*   Векторизация и семантический поиск по фрагментам документов с использованием векторной базы данных ChromaDB.
*   **Умный фолбек:** автоматическое переключение на встроенный легковесный поисковый движок на базе TF-IDF и косинусного сходства при отсутствии запущенной БД ChromaDB, что гарантирует работу RAG в любых условиях.

### 5. 🔑 Безопасность и хранение данных (Auth & DBMS)
*   Надежная регистрация и авторизация пользователей по стандарту JWT (JSON Web Tokens).
*   Сохранение конфигурации агентов, структуры графа, файлов и логов в асинхронную базу данных SQLite.

---

## 📁 Структура проекта / Project Structure

```bash
multi-agent-platform/
├── backend/                        # Асинхронный backend-сервер на FastAPI
│   ├── app/                        # Исходный код приложения
│   │   ├── agents/                 # Модули ИИ-агентов
│   │   │   └── orchestrator.py     # Оркестратор взаимодействия агентов
│   │   ├── routers/                # Эндпоинты API
│   │   │   ├── agents.py           # Управление агентами и их поведением
│   │   │   ├── auth.py             # Регистрация и JWT авторизация
│   │   │   ├── files.py            # Загрузка файлов и RAG индексация
│   │   │   └── integrations.py     # Внешние сервисы и интеграции
│   │   ├── auth.py                 # Логика шифрования и генерации JWT
│   │   ├── database.py             # Инициализация SQLAlchemy и aiosqlite
│   │   ├── encryption.py           # Утилиты хеширования паролей
│   │   ├── models.py               # Описание ORM-моделей базы данных
│   │   ├── rag.py                  # Движок индексации и поиска (RAG)
│   │   ├── sandbox.py              # Песочница выполнения Python-кода
│   │   ├── schemas.py              # Схемы валидации данных (Pydantic V2)
│   │   └── main.py                 # Точка входа в FastAPI приложение
│   ├── requirements.txt            # Зависимости Python
│   └── run.py                      # Скрипт локального запуска бэкенда
├── frontend/                       # Клиентское приложение Next.js (React)
│   ├── src/                        # Исходный код фронтенда
│   │   ├── app/                    # Страницы и роутинг (App Router)
│   │   ├── components/             # React компоненты интерфейса
│   │   │   ├── AgentGraph.tsx      # Визуальный граф агентов (XYFlow)
│   │   │   ├── AnimatedEdge.tsx    # Стилизованные ребра графа
│   │   │   ├── AuthModal.tsx       # Окно входа / регистрации
│   │   │   ├── ChatPanel.tsx       # Панель диалога с оркестратором
│   │   │   ├── ResultPanel.tsx     # Отображение артефактов песочницы
│   │   │   ├── SettingsPanel.tsx   # Управление ключами и конфигурацией
│   │   │   ├── TerminalLogs.tsx    # Панель вывода консоли бэкенда
│   │   │   └── WidgetHeader.tsx    # Заголовки панелей интерфейса
│   │   └── store/                  # Глобальное состояние
│   │       └── useStore.ts         # Zustand-хранилище сессии и графа
│   ├── package.json                # Конфигурация проекта и npm скрипты
│   └── tsconfig.json               # Конфигурация TypeScript
```

---

## 🔒 Безопасность и архитектурные решения / Security & Architecture

1.  **Единый коммерческий стандарт комментариев:** В основном проекте весь исходный код снабжен комментариями на английском языке (English codebase comments). В текущей демонстрационной версии (Showcase) комментарии к коду оставлены на русском языке исключительно для ознакомительных целей, чтобы упростить рекрутерам и техническим специалистам детальный разбор архитектурных решений.
2.  **Изоляция CORS & Сетевая безопасность:** На бэкенде настроена middleware-защита для обмена данными только с доверенными адресами (в целях локальной разработки разрешены все хосты `*`, для продакшена конфигурируется конкретный домен фронтенда).
3.  **Хранение секретов:** Конфиденциальные данные (API-ключи, JWT секреты) хранятся исключительно в файлах `.env` и добавлены в `.gitignore`, что предотвращает их случайное попадание в публичный репозиторий GitHub.

---

## 📄 Лицензия / License

Данный проект защищен авторским правом и поставляется под закрытой лицензией **Proprietary & Confidential License**. Исходный код предоставлен исключительно в ознакомительных целях для оценки потенциальными работодателями и рекрутерами. Любое коммерческое или стороннее использование, копирование или распространение строго запрещено.

This project is protected by copyright and is distributed under a **Proprietary & Confidential License**. The source code is provided solely for evaluation and review by potential employers or recruiters. Any commercial or third-party usage, copying, or distribution is strictly prohibited.
